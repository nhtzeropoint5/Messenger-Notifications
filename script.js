var notify = document.querySelector('.notifications');
var btn = document.querySelector('.btn');
btn.addEventListener('click', active);

function active(){
    var add = Number(notifications.getAttribute('data-count'));
}